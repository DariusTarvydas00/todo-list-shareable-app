import {createLogger, createStore} from "vuex";

const store = createStore({
  modules: {
  },
});

export default createStore({
  state: {},
  getters: {},
  mutations: {
    // SET_USER (state, user) {
    // }
  },
  actions: {},
  modules: {},
});
